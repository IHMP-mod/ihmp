
GER_collaboration_on_NOR = {
	allowed = {
		tag = GER
	}
	
	enable = {
		tag = GER
		NOR = {
			exists = yes
			has_capitulated = no
			NOT = { is_ally_with = GER }
		}
	}
	
	abort_when_not_enabled = yes

}
